import type { FC } from "react";

const StepHarmonize: FC = () => (
  <section>
    <h2>3. Harmonize</h2>
    <p>Placeholder UI for progress indicators, per-column status, and cancel/resume controls.</p>
  </section>
);

export default StepHarmonize;
