# Security: require npm packages to be at least 7 days old before install.
# This is an important supply-chain protection against newly published compromised packages.
min-release-age=7

# Security: fail installs when npm is too old to enforce the package age gate.
engine-strict=true
