import type { FC } from "react";

const StepUpload: FC = () => (
  <section>
    <h2>1. Upload Data</h2>
    <p>Placeholder UI for drag-and-drop uploader, file validation, and analysis trigger.</p>
  </section>
);

export default StepUpload;
