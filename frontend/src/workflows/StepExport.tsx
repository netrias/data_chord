import type { FC } from "react";

const StepExport: FC = () => (
  <section>
    <h2>5. Export & Finish</h2>
    <p>Placeholder UI for download buttons, summary statistics, and workflow restart.</p>
  </section>
);

export default StepExport;
