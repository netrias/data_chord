import type { FC } from "react";

const StepReview: FC = () => (
  <section>
    <h2>4. Review & Approve Results</h2>
    <p>Placeholder UI for batch navigation, cell detail modal triggers, and curator actions.</p>
  </section>
);

export default StepReview;
