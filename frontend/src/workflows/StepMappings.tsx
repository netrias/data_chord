import type { FC } from "react";

const StepMappings: FC = () => (
  <section>
    <h2>2. Review & Confirm Column → Model Mappings</h2>
    <p>Placeholder UI for column list, AI confidence badges, overrides, and data preview.</p>
  </section>
);

export default StepMappings;
